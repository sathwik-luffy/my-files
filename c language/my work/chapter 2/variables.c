#include <stdio.h>
    int main(){
        int a= 5;//declare and initialisze 'a' with 10
        int b=a;
        //%d is called formate specifier
        printf("the value of a is %d and value of b is %d", a,b);

     return 0;
}
