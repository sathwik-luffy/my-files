#include <stdio.h>
    int main(){
        int a = 64;
        int* b = &a;
                printf("the adress of i is %u\n",&a);

        printf("the value of i is %d",*b);

     return 0;
}
